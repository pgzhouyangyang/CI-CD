name: CI
on:
  push:
    branches: [ dev ]
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
      - uses: actions/checkout@v2

      - name: Install 
        run: |
          npm install
          npm run lint
          npm run build
